// prisma/schema.prisma  ← REPLACE your entire schema.prisma with this
// FIX: Added stripeCustomerId String? to User model
// After replacing this file, run: npx prisma migrate dev --name add_stripe_customer_id
// Then push to GitHub — Render will apply the migration automatically on redeploy

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Plan {
  FREE
  BASIC
  PRO
}

enum Role {
  STUDENT
  ADMIN
}

model User {
  id               String     @id @default(cuid())
  name             String
  email            String     @unique
  password         String
  plan             Plan       @default(FREE)
  role             Role       @default(STUDENT)
  stripeCustomerId String?                        // ← ADDED — was missing
  createdAt        DateTime   @default(now())
  updatedAt        DateTime   @updatedAt
  progress         Progress[]
}

model Course {
  id          String   @id @default(cuid())
  title       String
  language    String
  color       String   @default("#534AB7")
  level       String   @default("Beginner")
  description String
  published   Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  lessons     Lesson[]
}

model Lesson {
  id         String     @id @default(cuid())
  courseId   String
  course     Course     @relation(fields: [courseId], references: [id], onDelete: Cascade)
  lessonNo   Int
  title      String
  duration   String     @default("10 min")
  videoId    String
  accessPlan Plan       @default(BASIC)
  published  Boolean    @default(true)
  createdAt  DateTime   @default(now())
  updatedAt  DateTime   @updatedAt
  progress   Progress[]

  @@unique([courseId, lessonNo])
}

model Progress {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  lessonId  String
  lesson    Lesson   @relation(fields: [lessonId], references: [id], onDelete: Cascade)
  done      Boolean  @default(false)
  updatedAt DateTime @updatedAt

  @@unique([userId, lessonId])
}
